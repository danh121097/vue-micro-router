# Changelog


## v1.1.2

### ✨ Features

- VitePress site + CI/CD + auto-release + Cloudflare deploy

- Make the inspector an opt-in lazy chunk

### 🐛 Bug Fixes

- Give a dialog focus when it has no autofocus input

- Bind swipe-back to an element, not a fragment anchor

- Keep focus in a persistent dialog on backdrop press

- Bind to the page stack, not the consumer's app root

### 📚 Documentation

- Date the 1.1.0 release

### 🚀 Performance

- Scan focusable candidates instead of every node

- Hint the compositor while a page slide runs

- Drop the universal selector from the navigation lock

- Stop echoing page state back down as props

### 🚨 Tests

- Add mount + render-count net for the four SFCs

## v1.0.63

Initial documented release — VitePress docs site, GitHub CI/CD, automated release tagging, and Cloudflare Pages deploy.

Future versions are prepended above this entry automatically by CI from conventional commits.
